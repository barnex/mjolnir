package helheim

var (
	groups []*Group
	nodes  []*Node
)

// Run the scheduler. Infinite loop.
func RunSched() {
	/*FillNodes()
	for {
		select {
		case done := <-finish:
			Undispatch(done.Job, done.exitStatus)
			FillNodes()
		}
	}*/
}
